# push_swap-02
An optimized C sorting engine built for the 42 curriculum. Uses a doubly linked list to sort unique integers across two stacks with a restricted instruction set. Combines hardcoded permutation trees for small stacks ($\le 5$ elements) and a bitwise Radix Sort for larger datasets, all running on a custom coordinate compression layer.
